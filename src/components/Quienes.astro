<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Who We Are</title>

  <!-- Tipografías -->
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600&family=Open+Sans&display=swap" rel="stylesheet">

</head>
<body>
  <div id="quienes"></div>
  <section id="nosotros" class="seccion-nosotros">
    <div class="contenedor-nosotros">
      <div class="texto-nosotros">
        <h2>Who we are</h2>
        <p>
          At <strong>El Rincón del Repostero</strong>, we believe that baking is an art form. Every cake, cookie, and creation is born from the perfect blend of technique, passion, and respect for ingredients.
          <br><br>
          Since our inception, our goal has been to go beyond taste — to evoke emotions, memories, and experiences in every bite. With aesthetic care, traditional recipes, and a contemporary touch, we offer more than just sweets — we offer character.
        </p>
      </div>
      <div class="imagen-nosotros">
        <img src="somos.jpg" alt="Pastry chef working">
      </div>
    </div>
  </section>

  
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Open Sans', sans-serif;
      background-color: #fffaf7;
      color: #3b2e25;
    }

    .seccion-nosotros {
      padding: 100px 20px;
      background-image: url("fa.png"); /* Coloca tu imagen aquí */
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
      position: relative;
      z-index: 1;
    }

    .seccion-nosotros::before {
      content: "";
      position: absolute;
      inset: 0;
      background-color: rgba(255, 250, 247, 0.595);
      z-index: 0;
    }

    .contenedor-nosotros {
      max-width: 1200px;
      margin: 0 auto;
      display: flex;
      align-items: center;
      gap: 60px;
      flex-wrap: wrap;
      position: relative;
      z-index: 1;
      animation: fadeInUp 1s ease-in-out;
    }

    .texto-nosotros {
      flex: 1 1 500px;
      animation: slideInLeft 1.2s ease-out;
    }

    .texto-nosotros h2 {
      font-family: 'Playfair Display', serif;
      font-size: 3rem;
      margin-bottom: 25px;
      font-weight: 600;
      color: #3b2e25;
      position: relative;
    }

    .texto-nosotros h2::after {
      content: '';
      display: block;
      width: 60px;
      height: 4px;
      background-color: #a87532;
      margin-top: 12px;
    }

    .texto-nosotros p {
      font-size: 1.1rem;
      line-height: 1.9;
      color: #4b3d33;
      text-align: justify;
    }

    .imagen-nosotros {
      flex: 1 1 400px;
      text-align: center;
      animation: zoomIn 1.4s ease-out;
    }

    .imagen-nosotros img {
      width: 100%;
      max-width: 450px;
      border-radius: 20px;
      object-fit: cover;
      box-shadow: 0 12px 25px rgba(0, 0, 0, 0.15);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      animation: float 4s ease-in-out infinite;
    }

    .imagen-nosotros img:hover {
      transform: scale(1.05);
      box-shadow: 0 18px 30px rgba(0, 0, 0, 0.2);
    }

    @media (max-width: 900px) {
      .contenedor-nosotros {
        flex-direction: column-reverse;
        text-align: center;
      }

      .texto-nosotros h2::after {
        margin-left: auto;
        margin-right: auto;
      }

      .texto-nosotros p {
        text-align: center;
      }
    }

    /* Animaciones */
    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(40px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes slideInLeft {
      from { opacity: 0; transform: translateX(-60px); }
      to { opacity: 1; transform: translateX(0); }
    }

    @keyframes zoomIn {
      from { opacity: 0; transform: scale(0.9); }
      to { opacity: 1; transform: scale(1); }
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-8px); }
    }
  </style>

</body>
</html>
